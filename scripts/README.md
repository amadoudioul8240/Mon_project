# Scripts d'exploitation

Ce dossier contient les scripts utilitaires du projet qui ne font pas partie d'un agent specifique.

## Contenu

- `setup.sh` : construction de la stack Docker, demarrage des conteneurs et chargement de `init.sql`.

## Usage

Depuis la racine du depot:

```bash
bash scripts/setup.sh
```
