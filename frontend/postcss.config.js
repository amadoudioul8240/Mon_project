module.exports = {
  plugins: {
    // Tailwind génère les classes utilitaires à partir du code source.
    tailwindcss: {},
    // Autoprefixer ajoute automatiquement les préfixes CSS nécessaires selon les navigateurs ciblés.
    autoprefixer: {},
  },
};
